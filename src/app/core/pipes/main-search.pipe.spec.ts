import { MainSearchPipe } from './main-search.pipe';

describe('MainSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MainSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
